// dsh-desktop-conversation-navigator - client-only desktop enhancement.
// The host half exists so the package can be mounted through the same managed
// patch layer as the other desktop-owned plugins.
export const name = 'dsh-desktop-conversation-navigator'
export const inject = []

export function apply() {}
