{
  "identifier": "dsh-ui",
  "description": "Window-control permissions for the embedded DSH web UI (remote origin, main window only): its injected custom title bar needs drag + minimize/maximize/close and resize events to swap the maximize icon. No other IPC is exposed to the remote UI.",
  "local": false,
  "windows": ["main"],
  "remote": {
    "urls": ["http://127.0.0.1:*", "http://localhost:*"]
  },
  "permissions": [
    "core:window:allow-start-dragging",
    "core:window:allow-internal-toggle-maximize",
    "core:window:allow-minimize",
    "core:window:allow-toggle-maximize",
    "core:window:allow-close",
    "core:window:allow-is-maximized",
    "core:event:default"
  ]
}
